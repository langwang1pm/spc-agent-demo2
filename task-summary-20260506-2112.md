# 监控任务功能开发 - 任务摘要

## 时间
2026-05-06 21:12

## 需求
重新实现创建监控任务功能：
1. 任务配置预览（弹窗中显示监控任务各个字段信息）
2. 点击OK后，先保存分析配置到analysis_configs表
3. 再创建监控任务到monitor_tasks表，保存数据源ID、分析配置ID、最新数据快照（一维数组）

## 已完成的修改

### 后端

#### 1. models.py
- MonitorTask表新增 `latest_data` 字段（JSON类型），存储最新一次查询到的数据（一维数组）

#### 2. schemas.py
- MonitorTaskCreate 结构调整：
  - 移除了 `analysis_config_id`（由后端自动生成）
  - 新增 `chart_type`、`subgroup_size`、`confidence_level`（从前端配置获取）

#### 3. monitor.py（核心改动）
- 新增 `_get_latest_data_from_source()` 函数，支持从 SYSTEM/FILE/MANUAL 三种数据源获取最新数据，返回一维数组
- `create_monitor_task` 接口重构：
  1. 创建 AnalysisConfig 记录
  2. 获取最新数据快照
  3. 创建 MonitorTask 记录（关联 analysis_config_id 和 latest_data）

### 前端

#### 1. api/monitor.ts
- `createMonitorTask` 接口参数调整为：name, data_source_id, chart_type, subgroup_size, confidence_level, interval_seconds

#### 2. 新增 CreateMonitorModal.vue
- 弹窗组件，显示任务配置预览（数据源ID、图表类型、子组大小、置信水平、监控间隔）
- 任务名称输入框（自动生成带时间戳的名称）
- 监控间隔配置
- OK按钮提交到后端

#### 3. MainPage.vue
- 引入 CreateMonitorModal 组件
- 添加 showCreateMonitor 状态变量
- `handleCreateMonitor` 改为打开 CreateMonitorModal 弹窗
- 在模板中添加 CreateMonitorModal 组件

## 补充修改 (21:38)

### MainPage.vue
- 移除分析配置区域的「自动刷新」和「刷新间隔」字段（系统对接方式下不再显示）
- `analysisConfig` 对象移除 `autoRefresh`、`refreshInterval` 字段
- 自动刷新逻辑：系统对接数据源添加成功后，固定每 60 秒刷新一次 SPC 结果
- 移除相关的 CSS 样式（refresh-status、refresh-hint）

## Bug修复 (22:00)

### Bug 1: /spc/refresh 接口更新数据库 data_values
**问题**：系统对接数据源刷新时会覆盖数据库中的 data_values 字段
**修复**：移除 `db.commit()` 和 `db.refresh()` 调用，刷新操作只查询数据不在数据库落库

### Bug 2: 自动刷新时分析配置丢失
**问题**：`performAutoRefresh` 只传了 data_source_id，没有传分析配置参数
**修复**：
- 后端 `/spc/refresh` 接口新增参数：`chart_type`, `subgroup_size`, `confidence_level`, `show_rules`, `show_prediction`
- 前端 `refreshSPCData` API 接受配置参数
- `performAutoRefresh` 传递当前 `analysisConfig` 中的配置值

## 待验证
- [x] 数据库迁移脚本已创建：`scripts/migrate_add_latest_data.sql`
- [ ] 前端运行 `npm run dev` 测试完整流程
- [ ] 验证系统对接数据源刷新后，数据库 data_values 未被更新
- [ ] 验证修改分析配置后刷新，使用的配置是最新设置

## 关键文件
- `D:\PycharmProject\spc-agent-demo\backend\app\models\models.py`
- `D:\PycharmProject\spc-agent-demo\backend\app\schemas\schemas.py`
- `D:\PycharmProject\spc-agent-demo\backend\app\api\monitor.py`
- `D:\PycharmProject\spc-agent-demo\frontend\src\api\monitor.ts`
- `D:\PycharmProject\spc-agent-demo\frontend\src\components\CreateMonitorModal.vue`
- `D:\PycharmProject\spc-agent-demo\frontend\src\views\MainPage.vue`
- `D:\PycharmProject\spc-agent-demo\scripts\migrate_add_latest_data.sql`