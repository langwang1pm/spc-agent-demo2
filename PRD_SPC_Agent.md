# SPC Agent - 统计过程控制智能助手
## 产品需求文档（PRD）
**版本：** v1.0
**日期：** 2026-05-11
**作者：** SPC Agent 团队

---

## 0.演示说明：

### 0.1 演示环境地址：
- http://124.222.98.162/

### 0.2 数据输入（测试数据）：

- 手动输入：

  25.3, 25.5, 25.4, 25.6, 25.2, 25.4, 25.6, 25.3, 25.5, 25.7, 25.2, 25.4, 25.5, 25.3, 25.6, 25.5, 25.7, 25.4, 25.6, 25.3, 25.3, 25.5, 25.7, 25.4, 25.6, 25.6, 25.8, 25.5, 25.7, 25.4, 25.4, 25.6, 25.8, 25.5, 25.7, 25.7, 25.9, 25.6, 25.8, 25.5, 25.5, 25.7, 25.9, 25.6, 25.8, 25.8, 26.0, 25.7, 25.9, 25.6, 26.1, 26.3, 26.2, 26.4, 26.0, 26.2, 26.4, 26.1, 26.3, 26.5, 26.0, 26.2, 26.4, 26.1, 26.3, 26.3, 26.5, 26.2, 26.4, 26.1, 26.1, 26.3, 26.5, 26.2, 26.4, 25.2, 25.1, 25.3, 25.0, 25.4, 25.0, 24.9, 25.1, 24.8, 25.2, 24.7, 24.6, 24.8, 24.5, 24.9, 24.5, 24.4, 24.6, 24.3, 24.7, 24.2, 24.1, 24.3, 24.0, 24.4, 25.5, 25.4, 25.6, 25.3, 25.7, 25.4, 25.6, 25.5, 25.7, 25.3, 25.6, 25.8, 25.7, 25.9, 25.5, 25.7, 25.9, 25.8, 26.0, 25.6, 27.2, 27.5, 27.3, 27.6, 27.4, 23.1, 23.3, 23.2, 23.4, 23.0, 25.3, 25.5, 25.4, 25.6, 25.2, 25.4, 25.6, 25.3, 25.5, 25.7, 25.2, 25.4, 25.5, 25.3, 25.6, 25.5, 25.7, 25.4, 25.6, 25.3

- 导入文件：
  可以下载模板，然后将模板直接上传（模板中有示例数据）

- 系统对接:

  数据源类型：选择`数据库`

  连接配置：

  ```json
  {"DB_type": "postgresql", "host": "sh-postgres-h3849b66.sql.tencentcdb.com", "port": 21656, "dbname": "icoastline", "user": "le_owner", "password": "PG^le=[2021)"}
  ```

  数据查询：

  ```sql
  select measure_value_a from spc_agent_demo.qms_inspection_detail qid where qid.inspection_type = 'IQC' order by create_time desc limit 50;
  ```

  



## 1. 产品概述

### 1.1 项目背景

SPC（Statistical Process Control，统计过程控制）是制造业质量管理的核心技术，通过对生产过程数据进行统计分析，实时监控过程稳定性，及时发现异常波动，是实现"事前预防"而非"事后检验"质量管理的关键手段。

传统的 SPC 软件通常存在以下痛点：
- **部署复杂**：需要专业软件和本地安装维护
- **分析门槛高**：需要统计专业人员操作
- **响应滞后**：异常发现不及时，告警机制不完善
- **数据孤岛**：与生产系统、通讯工具割裂

### 1.2 产品定位

**SPC Agent** 是一款基于飞书的 SPC 智能分析系统，旨在将复杂的统计过程控制能力普惠化、平台化。通过与企业通讯工具飞书的深度集成，让质量工程师、生产管理者在熟悉的飞书环境中即可完成 SPC 分析、监控、告警全流程。

核心价值：
- **低门槛**：图形化操作，无需统计背景
- **高效率**：AI 自动解读图表，秒级响应
- **实时性**：自动化定时监控，数据驱动决策
- **协同性**：告警直达飞书群，信息无缝流转

### 1.3 目标用户

| 用户角色 | 使用场景 |
|---------|---------|
| 质量工程师（QE） | 创建 SPC 分析任务、解读控制图、处置异常 |
| 生产主管 | 接收告警通知、查看产线稳定性状态 |
| 工艺工程师 | 配置分析参数、优化控制限 |
| 管理层 | 查看质量趋势报告、关注关键指标 |

### 1.4 技术架构

```
┌─────────────────────────────────────────┐
│              飞书客户端                   │
│     （消息通知 / Web 访问入口）           │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│           SPC Agent Frontend            │
│   Vue 3 + TypeScript + Ant Design Vue   │
│   + ECharts + Pinia + vue-router        │
└──────────────┬──────────────────────────┘
               │ HTTP / REST API
               ▼
┌─────────────────────────────────────────┐
│           SPC Agent Backend             │
│     FastAPI (Python) + SQLite           │
│                                         │
│  ┌─────────────┐  ┌──────────────────┐  │
│  │ SPC 计算引擎 │  │ AI 分析服务      │  │
│  │ (9种控制图)  │  │ (对接 Dify)      │  │
│  └─────────────┘  └──────────────────┘  │
│  ┌─────────────┐  ┌──────────────────┐  │
│  │ 飞书通知服务 │  │ 定时监控调度器    │  │
│  │ (httpx)     │  │ (APScheduler)    │  │
│  └─────────────┘  └──────────────────┘  │
│  ┌─────────────┐  ┌──────────────────┐  │
│  │ 系统数据查询 │  │ 导出服务         │  │
│  │ (DB/ERP/MES)│  │ (Chart/CSV/PDF)  │  │
│  └─────────────┘  └──────────────────┘  │
└──────────────┬──────────────────────────┘
               │
     ┌─────────┼──────────┬──────────────┐
     ▼         ▼          ▼              ▼
 ┌───────┐ ┌───────┐ ┌────────┐ ┌──────────┐
 │SQLite │ │ Dify  │ │ 飞书   │ │外部系统  │
 │本地库 │ │ AI平台│ │ Webhook│ │ERP/MES等 │
 └───────┘ └───────┘ └────────┘ └──────────┘
```

---

## 2. 功能模块

### 2.1 模块总览

| 模块 | 功能描述 | 优先级 |
|-----|---------|--------|
| SPC 分析 | 9种控制图计算与可视化 | P0 |
| 数据输入 | 手动录入 / 文件上传 / 系统对接 | P0 |
| AI 智能分析 | AI 自动解读 SPC 结果 | P1 |
| 异常监控 | 定时任务 + 飞书告警 | P0 |
| 导出报告 | 图表 / CSV / PDF 导出 | P2 |
| 系统设置 | 飞书连接 / Dify 配置 / 数据源配置 | P0 |

---

### 2.2 SPC 分析引擎

#### 2.2.1 支持的控制图类型

| 图表类型 | 代码 | 适用场景 |
|---------|------|---------|
| X-bar & R 控制图 | `xbar_r` | 小样本（n≤10）计量型数据 |
| X-bar & S 控制图 | `xbar_s` | 大样本（n>10）计量型数据 |
| X & MR（单值移动极差）图 | `x_mr` | 单值数据、自动化检测数据 |
| P 控制图（比率型） | `p` | 不合格率等比率数据 |
| NP 控制图（计数型） | `np` | 固定样本量的不合格数 |
| C 控制图（计数型） | `c` | 单位缺陷数（样本量恒定） |
| U 控制图（计数型） | `u` | 单位缺陷数（样本量可变） |
| 累积和控制图 | `cusum` | 小偏移检测（灵敏度高） |
| 指数加权移动平均控制图 | `ewma` | 过程持续监控（平滑处理） |

#### 2.2.2 SPC 计算核心逻辑

**计算指标：**
- **中心线（CL）**：过程均值
- **控制上限（UCL）**：CL + 3σ
- **控制下限（LCL）**：CL - 3σ
- **sigma 估计**：基于子组内变异的统计估计
- **过程能力指数 Cp/Cpk**（计量型）
- **西格玛水平 Z-Score**

**判异规则：**
系统内置 Western Electric Rules（8条判异规则），自动标注超限点：
1. 任何点落在 3σ 区域外
2. 连续 3 点中有 2 点落在 2σ 区域外
3. 连续 5 点中有 4 点落在 1σ 区域外
4. 连续 8 点落在中心线同一侧
5. 连续 6 点递增或递减
6. 连续 14 点交替上下
7. 连续 15 点落在 1σ 区域内（过程过稳）
8. 连续 8 点落在 1σ 区域外（过程不稳定）

**异常分类：**
- 🔴 **RED**：落在 UCL/LCL 之外 → 立即告警
- 🟡 **YELLOW**：违反判异规则但未超限 → 关注告警
- 🟢 **GREEN**：过程正常

#### 2.2.3 图表展示

前端基于 **ECharts** 渲染 SPC 控制图，包含：
- 折线图主体（数据点）
- UCL / CL / LCL 三条控制线
- 异常点颜色标注（RED/YELLOW/GREEN）
- 鼠标悬停提示（tooltip）：显示子组号、数值、超限信息
- 底部标注：关键统计数据（均值、sigma、异常点数量、Cpk 等）

---

### 2.3 数据输入

#### 2.3.1 手动录入
- 前端表格输入界面
- 支持批量粘贴（从 Excel 复制数据）
- 支持子组配置（子组大小 n、采样时间戳）

#### 2.3.2 文件上传
- 支持格式：`CSV`、`XLSX`
- 上传后自动解析列名和数据
- 支持预览和字段映射

#### 2.3.3 外部系统对接（系统查询）
后端服务 `system_query.py` 支持从以下来源获取数据：
- **关系型数据库**（MySQL / PostgreSQL / SQL Server）
- **ERP 系统**（通过 SQL 查询）
- **MES 系统**
- **PLC 数据采集**（通过配置化查询）
- **HTTP API 接口**（通用 HTTP 请求获取 JSON 数据）

配置界面支持：
- 连接字符串管理（加密存储）
- 自定义 SQL 查询模板
- 数据映射规则
- 定时同步配置

---

### 2.4 AI 智能分析（对接 Dify）

#### 2.4.1 分析流程

```
用户触发分析
     │
     ▼
调用 Dify API（携带 SPC 结果 JSON）
     │
     ▼
Dify Agent 解析数据 → 生成分析文本
     │
     ▼
返回前端展示 / 追加到飞书告警卡片
```

#### 2.4.2 Dify 应用要求

Dify 应用需要接收以下输入信息：
- 控制图类型
- 数据集（数值序列）
- SPC 统计结果（均值、UCL、LCL、异常点位置）
- 异常类型（RED/YELLOW 点详情）
- 过程能力（Cp/Cpk）

Dify 应用应输出：
- 自然语言分析报告（中文）
- 异常原因推断
- 建议处置措施

#### 2.4.3 后端实现

```python
# ai_agent.py 核心逻辑
- 构建 Dify API 请求（POST /v1/run）
- 携带 API-Key 认证
- 流式响应支持（SSE）
- 超时和重试处理
```
---

### 2.5 异常监控与告警

#### 2.5.1 监控任务

用户可在前端配置监控任务，设置参数：
- **监控对象**：选择分析配置
- **数据源**：指定系统查询配置
- **执行周期**：`*/n` 分钟（支持任意间隔）
- **触发条件**：
  - 数据点超限
  - 违反判异规则
  - Cpk 低于阈值
  - 连续 N 个周期无数据

#### 2.5.2 告警机制

**飞书消息卡片（Card Message）：**

当监控任务触发时，后端调用飞书 API 发送卡片消息，包含：
- 告警等级（🔴 严重 / 🟡 警告）
- 产线/工序名称
- 控制图类型
- 触发时间
- 异常描述（AI 自动生成）
- 快捷操作按钮：「查看图表」「查看详情」「处理」「忽略」

**卡片模板示例：**
```
┌──────────────────────────────────────┐
│ 🔴 [SPC告警] X-bar&R 控制图异常       │
├──────────────────────────────────────┤
│ 产线：组装车间-A线                    │
│ 指标：注塑压力                        │
│ 时间：2026-05-11 09:15               │
│ 异常：2个点超出UCL（超限率33%）       │
├──────────────────────────────────────┤
│ AI分析：注塑压力短时间出现两次大幅上  │
│ 升波动，可能由模具松动或原料批次差异  │
│ 引起。建议立即检查模具固定情况和原    │
│ 料供应商变更记录。                    │
├──────────────────────────────────────┤
│ [查看图表]  [查看详情]  [标记已处理] │
└──────────────────────────────────────┘
```

#### 2.5.3 调度器实现

后端 `scheduler.py` 使用 **APScheduler** 实现：
- 持久化调度任务（存储于 SQLite）
- 应用启动时自动恢复所有启用状态的任务
- 调度器在独立线程中运行，不阻塞主服务
- 任务执行时调用 SPC 计算 → 判断触发条件 → 发送告警

---

### 2.6 导出功能

支持三种导出方式：

| 导出类型 | 格式 | 内容 |
|---------|------|------|
| 控制图 | PNG | ECharts 渲染的图表图片 |
| 原始数据 | CSV | 包含子组数据的 Excel |
| 分析报告 | PDF | 图表 + AI 分析文本 + 统计汇总 |

---

### 2.7 系统设置

#### 2.7.1 飞书配置
- **App ID / App Secret**：企业自建应用的凭证
- **Webhook 地址**：群机器人 Webhook（用于卡片消息）
- **接收人配置**：告警消息的默认接收人或群

#### 2.7.2 Dify 配置
- **Base URL**：Dify 服务地址
- **API-Key**：认证密钥
- **App Key / User**：Dify 应用标识

#### 2.7.3 数据源管理
- 添加 / 编辑 / 删除外部数据源连接
- 连接测试功能
- 查询语句验证

---

## 3. 数据库设计

### 3.1 核心表结构

#### `spc_results` - SPC 分析结果
| 字段 | 类型 | 说明 |
|-----|------|------|
| id | INTEGER PK | 主键 |
| name | VARCHAR(200) | 分析名称 |
| chart_type | VARCHAR(50) | 控制图类型 |
| data | TEXT | JSON格式的原始数据 |
| stats | TEXT | JSON格式的SPC统计结果 |
| control_limits | TEXT | JSON格式的控制限 |
| ai_analysis | TEXT | AI分析文本 |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 更新时间 |

#### `analysis_configs` - 分析配置
| 字段 | 类型 | 说明 |
|-----|------|------|
| id | INTEGER PK | 主键 |
| name | VARCHAR(200) | 配置名称 |
| chart_type | VARCHAR(50) | 控制图类型 |
| config | TEXT | JSON格式的配置参数 |
| created_by | VARCHAR(100) | 创建者 |
| created_at | DATETIME | 创建时间 |

#### `monitor_tasks` - 监控任务
| 字段 | 类型 | 说明 |
|-----|------|------|
| id | INTEGER PK | 主键 |
| name | VARCHAR(200) | 任务名称 |
| analysis_config_id | INTEGER FK | 关联的分析配置ID |
| data_source_id | INTEGER FK | 关联的数据源ID |
| schedule_expr | VARCHAR(100) | 调度表达式 |
| trigger_conditions | TEXT | JSON格式的触发条件 |
| enabled | BOOLEAN | 是否启用 |
| last_run_at | DATETIME | 上次执行时间 |
| created_at | DATETIME | 创建时间 |

#### `data_sources` - 数据源
| 字段 | 类型 | 说明 |
|-----|------|------|
| id | INTEGER PK | 主键 |
| name | VARCHAR(200) | 数据源名称 |
| source_type | VARCHAR(50) | 类型（database/erp/mes/http） |
| connection_config | TEXT | JSON格式的连接配置 |
| query_template | TEXT | 查询模板SQL |
| created_at | DATETIME | 创建时间 |

#### `system_settings` - 系统配置（KV存储）
| 字段 | 类型 | 说明 |
|-----|------|------|
| key | VARCHAR(100) PK | 配置键 |
| value | TEXT | 配置值（加密存储敏感字段） |
| description | VARCHAR(500) | 配置描述 |
| updated_at | DATETIME | 更新时间 |

---

## 4. API 接口设计

### 4.1 数据源接口（`/api/data`）
| 方法 | 路径 | 说明 |
|-----|------|------|
| GET | `/api/data/sources` | 获取所有数据源列表 |
| POST | `/api/data/sources` | 创建数据源 |
| PUT | `/api/data/sources/{id}` | 更新数据源 |
| DELETE | `/api/data/sources/{id}` | 删除数据源 |
| POST | `/api/data/sources/test` | 测试数据源连接 |
| POST | `/api/data/sources/query` | 执行数据源查询 |

### 4.2 SPC 接口（`/api/spc`）
| 方法 | 路径 | 说明 |
|-----|------|------|
| POST | `/api/spc/calculate` | 执行 SPC 计算 |
| GET | `/api/spc/results` | 获取分析结果列表 |
| GET | `/api/spc/results/{id}` | 获取指定结果详情 |
| DELETE | `/api/spc/results/{id}` | 删除分析结果 |
| POST | `/api/spc/analyze` | 获取 AI 分析结果 |
| GET | `/api/spc/chart-data/{id}` | 获取图表渲染数据 |

### 4.3 导出接口（`/api/export`）
| 方法 | 路径 | 说明 |
|-----|------|------|
| POST | `/api/export/chart` | 导出图表（PNG） |
| POST | `/api/export/csv` | 导出数据（CSV） |
| POST | `/api/export/pdf` | 导出报告（PDF） |

### 4.4 监控接口（`/api/monitor`）
| 方法 | 路径 | 说明 |
|-----|------|------|
| GET | `/api/monitor/tasks` | 获取监控任务列表 |
| POST | `/api/monitor/tasks` | 创建监控任务 |
| PUT | `/api/monitor/tasks/{id}` | 更新监控任务 |
| DELETE | `/api/monitor/tasks/{id}` | 删除监控任务 |
| POST | `/api/monitor/tasks/{id}/toggle` | 启用/禁用任务 |
| POST | `/api/monitor/tasks/{id}/run` | 手动触发执行 |
| GET | `/api/monitor/history` | 获取任务执行历史 |

### 4.5 分析配置接口（`/api/analysis`）
| 方法 | 路径 | 说明 |
|-----|------|------|
| GET | `/api/analysis/configs` | 获取分析配置列表 |
| POST | `/api/analysis/configs` | 创建分析配置 |
| PUT | `/api/analysis/configs/{id}` | 更新分析配置 |
| DELETE | `/api/analysis/configs/{id}` | 删除分析配置 |

### 4.6 系统设置接口（`/api/settings`）
| 方法 | 路径 | 说明 |
|-----|------|------|
| GET | `/api/settings` | 获取所有系统设置 |
| PUT | `/api/settings` | 更新系统设置 |
| GET | `/api/settings/feishu/test` | 测试飞书连接 |
| GET | `/api/settings/dify/test` | 测试 Dify 连接 |

---

## 5. 前端页面设计

### 5.1 页面路由

| 路由 | 页面 | 说明 |
|-----|------|------|
| `/` | MainPage.vue | 主页（分析入口 + 侧边栏） |
| `/data` | DataSource.vue | 数据源管理 |
| `/chart` | Chart.vue | SPC 控制图展示 |
| `/monitor` | Monitor.vue | 监控任务管理 |

### 5.2 MainPage 主页面布局

```
┌─────────────────────────────────────────────────────────┐
│  LOGO  SPC Agent          [监控中心] [⚙ 设置] [用户]    │
├────────────┬────────────────────────────────────────────┤
│            │                                            │
│  ◉ 新建分析 │         数据输入区域                       │
│            │   ┌────────────────────────────────┐      │
│  📊 SPC分析 │   │ [手动输入] [上传文件] [系统接入] │      │
│            │   │                                │      │
│  📁 数据管理 │   │   <textarea / file picker>    │      │
│            │   │                                │      │
│  🔔 监控中心 │   └────────────────────────────────┘      │
│            │                                            │
│  ⚙ 系统设置 │   分析参数配置                             │
│            │   图表类型: [X-bar & R ▾] 子组大小: [5]    │
│            │                                            │
│            │         [开始分析]                          │
│            │                                            │
│            ├────────────────────────────────────────────┤
│            │         分析结果 / 控制图展示区域            │
│            │   ┌────────────────────────────────────┐  │
│            │   │         ECharts 控制图              │  │
│            │   │                                    │  │
│            │   │  UCL ─ ─ ─ ─ ─ ─ ─ ─               │  │
│            │   │      ● ●    ●                      │  │
│            │   │  CL  ─────────────────────         │  │
│            │   │      ● 🔴 ●    ● 🔴 ●              │  │
│            │   │  LCL ─ ─ ─ ─ ─ ─ ─ ─               │  │
│            │   └────────────────────────────────────┘  │
│            │                                            │
│            │   AI 分析结论：<AI生成的自然语言报告>      │
│            │                                            │
│            │   [导出图表] [导出CSV] [导出PDF] [重新分析] │
│            │                                            │
└────────────┴────────────────────────────────────────────┘
```

### 5.3 核心组件

| 组件名 | 功能 |
|-------|------|
| `SPCChart.vue` | ECharts 控制图渲染 |
| `DataInput.vue` | 数据输入（手动/文件/系统） |
| `SettingsModal.vue` | 系统设置弹窗 |
| `CreateMonitorModal.vue` | 创建监控任务弹窗 |
| `MonitorCenterModal.vue` | 监控中心（任务列表+执行历史） |

---

## 6. 依赖技术栈

### 6.1 后端依赖

```
# Web 框架
fastapi==0.115.0
uvicorn[standard]==0.30.6

# 数据处理
numpy>=1.26.0
pandas>=2.0.0

# SPC 计算
spc-js==0.3.0

# 数据库
sqlalchemy==2.0.35
aiosqlite==0.20.0

# HTTP 客户端
httpx==0.27.2

# 调度器
apscheduler==3.10.4

# 图表导出
plotly>=5.22.0
kaleido==0.2.1

# 配置管理
pydantic-settings>=2.0.0

# 文件处理
python-multipart==0.0.9
openpyxl>=3.1.0
```

### 6.2 前端依赖

```json
{
  "vue": "^3.5.0",
  "vue-router": "^4.4.0",
  "pinia": "^2.2.0",
  "ant-design-vue": "^4.2.0",
  "echarts": "^5.5.0",
  "axios": "^1.7.0",
  "typescript": "^5.5.0"
}
```

---

## 7. 部署架构

### 7.1 部署模式

**推荐模式：单主机部署（Docker Compose）**

```
spc-agent-demo/
├── docker-compose.yml
├── backend/
│   └── Dockerfile
└── frontend/
    ├── Dockerfile
    └── nginx.conf
```

**环境变量配置（.env）：**
```env
# 数据库
DATABASE_URL=sqlite+aiosqlite:///./spc_agent.db

# 飞书
FEISHU_APP_ID=cli_xxxx
FEISHU_APP_SECRET=xxxx
FEISHU_WEBHOOK_URL=https://open.feishu.cn/open-apis/bot/v2/hook/xxxx

# Dify
DIFY_BASE_URL=https://your-dify-instance/v1
DIFY_API_KEY=app-xxxx
DIFY_APP_KEY=xxxx
DIFY_USER=xxx

# 服务
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000
FRONTEND_PORT=3000
```

### 7.2 性能指标目标

| 指标 | 目标值 |
|-----|-------|
| SPC 计算响应时间 | < 2s（1000个数据点） |
| 页面首次加载 | < 3s |
| API 并发处理能力 | ≥ 50 QPS |
| 告警消息送达延迟 | < 5s（飞书消息） |
| 定时任务调度精度 | ±5s |

---

## 8. 安全设计

| 安全项 | 实现方式 |
|-------|---------|
| 飞书凭证 | App Secret 存储于数据库，运行时解密 |
| 数据库 | SQLite 本地文件，文件权限控制 |
| API 认证 | 目前无认证（生产环境需添加） |
| SQL 注入 | 使用 SQLAlchemy ORM 参数化查询 |
| CORS | 限制为前端部署域名 |
| 敏感信息 | 数据源连接字符串加密存储 |

> ⚠️ **生产部署注意**：当前版本 API 无认证机制，建议在内网环境使用或补充认证层（如 JWT）。

---

## 9. 迭代计划

### Phase 1 - MVP（本项目当前状态）
- ✅ SPC 九种控制图计算
- ✅ 手动数据输入 + 文件上传
- ✅ AI 分析（Dify 接入）
- ✅ 飞书告警通知
- ✅ 定时监控任务

### Phase 2 - 增强版
- [ ] 多租户隔离
- [ ] JWT 认证 + RBAC 权限
- [ ] 数据源连接池和连接管理
- [ ] 历史数据趋势对比
- [ ] 移动端适配

### Phase 3 - 平台化
- [ ] SPC 知识库（规则引擎）
- [ ] 根因分析（RCA）集成
- [ ] 数据大屏
- [ ] OpenAPI 开放接口
- [ ] 插件化数据源扩展

---

## 10. 附录

### 10.1 术语表

| 术语 | 英文 | 说明 |
|-----|------|------|
| SPC | Statistical Process Control | 统计过程控制 |
| UCL | Upper Control Limit | 控制上限 |
| LCL | Lower Control Limit | 控制下限 |
| CL | Center Line | 中心线 |
| Cp | Process Capability | 过程能力指数（潜在） |
| Cpk | Process Capability Index | 过程能力指数（实际） |
| 子组 | Subgroup | 统计分组的数据单位 |
| 判异规则 | Western Electric Rules | 判断过程异常的行为规则 |
| Dify | - | 开源 LLM 应用开发平台 |

### 10.2 参考资料

- Western Electric Rules: https://en.wikipedia.org/wiki/Western_Electric_rules
- SPC 控制图选择指南: Montgomery, D.C. "Introduction to Statistical Quality Control"
- 飞书卡片消息文档: https://open.feishu.cn/document/uAjLw4CM/ukTMukTMukTM/bot-v2/bot-custom-discrete-card
